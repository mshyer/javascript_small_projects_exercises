let completed = lastPageRead == 400;

// prefer strict equality operator
// when mixing operators, enclose them in parentheses, except + 0 and **. / and * should be enclosed

let completed = (lastPageRead === 400);
