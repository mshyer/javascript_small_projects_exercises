// In the prev problem, multiplyBy5 reetains access to func and b
// After makePartialFunc has finished execution. This is because
// Both func and b are available in the closure. When a function is created,
// It retains access to variables defined in the function's outer scope.
